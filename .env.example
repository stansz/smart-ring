DATABASE_URL=postgresql://smart_ring:changeme@localhost:5432/smart_ring
RING_ADDRESS=
POSTGRES_PASSWORD=changeme

# BLE connect retry (R09 sleeps aggressively; tune per trigger)
# SYNC_ATTEMPTS=5          # sync_ring.py  — used by collector cron (manual uses 5)
# FIRST_CONTACT_ATTEMPTS=5 # first_contact.py — Admin-tab "First Contact" button
# BLE_CONNECT_TIMEOUT=30   # seconds, per-attempt GATT service-discovery timeout
