"""Shared test fixtures.

Intentionally minimal — most tests in this suite are unit tests against
pure functions (trap_score, BCD encoding, etc.) and don't need fixtures.
Add fixtures here as DB-backed tests come online (dedupe smoke, etc.).
"""
